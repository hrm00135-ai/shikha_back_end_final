# ─── App ──────────────────────────────────────────────────────
PORT=5000
HOST=0.0.0.0
NODE_ENV=development
SECRET_KEY=CLOUDINARY_URL=cloudinary://343867817715967:**********@dc4aoebux

# ─── Database ─────────────────────────────────────────────────
# MySQL (production) - Replace with your Hostinger database details
MYSQL_URL=mysql://u333005313_jcadmin:JewelCraft@2026!@localhost:3306/u333005313_jewelcraft

# SQLite is used automatically if MYSQL_URL is not set (dev only)

# ─── JWT ──────────────────────────────────────────────────────
JWT_SECRET_KEY=<32+ random chars>
JWT_ACCESS_TOKEN_EXPIRES_MINUTES=15
JWT_REFRESH_TOKEN_EXPIRES_DAYS=7

# ─── Email ────────────────────────────────────────────────────
MAIL_SERVER=smtp.gmail.com
MAIL_PORT=587
MAIL_USE_TLS=True
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_DEFAULT_SENDER=

# ─── Encryption (Fernet-compatible 32-byte base64 key) ────────
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
ENCRYPTION_KEY=<32+ random chars — same one you were using, or generate new since DB is fresh>

# ─── Cloudinary ───────────────────────────────────────────────
CLOUDINARY_CLOUD_NAME=dc4aoebux
CLOUDINARY_API_KEY=343867817715967
CLOUDINARY_API_SECRET=343867817715967

# ─── Metal Prices ─────────────────────────────────────────────
METAL_API_KEY=

# ─── Super Admin (used only during first migration) ───────────
SUPER_ADMIN_EMAIL=superadmin@jewelcraft.com
SUPER_ADMIN_PASSWORD=Admin@123456

# PORT=5000
# HOST=0.0.0.0
# NODE_ENV=development
# SECRET_KEY=anyrandomsecretstring123
# JWT_SECRET_KEY=anyrandomsecretstring123
# JWT_ACCESS_TOKEN_EXPIRES_MINUTES=15
# JWT_REFRESH_TOKEN_EXPIRES_DAYS=7